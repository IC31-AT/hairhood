# Copy to .env.local for local dev. Also set these in the Cloudflare Pages
# dashboard (Settings → Environment variables) for the deployed site.
# These are public/non-secret — the Sanity dataset is read-only from here.
VITE_SANITY_PROJECT_ID=ep0gakki
VITE_SANITY_DATASET=production
